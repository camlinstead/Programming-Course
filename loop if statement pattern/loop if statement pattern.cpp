	double ans;
	double avg;

	avg = (num1 + num2 + num3 + num4 + num5) / 5;


	ans = sqrt((pow((num1-avg),2)+pow((num2-avg),2)+pow((num3-avg),2)+pow((num4-avg),2)+pow((num5-avg),2)) / 5 );

	return ans;
	
